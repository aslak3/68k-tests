MEMORY
{
  	ROM(RX) : ORIGIN = 0x000000, LENGTH = 16K
	RAM(RWX) : ORIGIN = 0x0e0000, LENGTH = 32K
}

SECTIONS
{
	. = 0;
	vectors : { *(vectors) } > ROM
	.text 0x0400 : { *(.text) } > ROM
	.rodata : { *(.rodata) } > ROM
	.bss : { *(.bss) } > RAM
}
